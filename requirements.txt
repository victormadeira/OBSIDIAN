fastapi==0.115.0
uvicorn[standard]==0.30.0
python-multipart==0.0.9
pyyaml==6.0.1

# Vector / Embedding layer
chromadb>=0.5.0
sentence-transformers>=3.0.0

# Optional: better clustering (used by vector_service.get_embedding_clusters)
scikit-learn>=1.4.0

# Optional: OpenAI embeddings (only needed if OBSIDIAN_EMBEDDING_PROVIDER=openai)
# openai>=1.0.0
